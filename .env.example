# Invented empty placeholders used by README.md's separate nonempty examples.
# Prefer managed /connect provider store 2 or environment interpolation over
# plaintext TOML credentials. Never commit .env or a real credential.
COOKIE_AGENT_EXAMPLE_OPENAI_API_KEY=
COOKIE_AGENT_EXAMPLE_VERTEX_ACCESS_TOKEN=
COOKIE_AGENT_EXAMPLE_CUSTOM_API_KEY=
