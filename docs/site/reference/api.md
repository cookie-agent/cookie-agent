# Rust API Documentation

The workspace rustdoc output is published with each documentation version.

<a class="md-button" href="../../api/">Open workspace API documentation</a>

The API index includes all ten workspace crates and documents public items
only. Build it locally together with this site by running:

```sh
./scripts/build-docs.sh
```
