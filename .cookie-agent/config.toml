# Bootstrap documentation fixture for strict configuration parsing.
# The providers map may be absent or empty. With an empty per-user provider store
# and no effective custom provider, startup opens the zero-model TUI and keeps
# /connect available. Existing global stored connections still apply.

[delegation]
max_depth = 3

[server]
host = "127.0.0.1"
port = 17419

[providers]
